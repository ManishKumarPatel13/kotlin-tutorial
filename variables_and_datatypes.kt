fun main(){
    //  In java all primitive data types are started with lowercase letter and non-primitive with uppercase letter but here all are non-primitive
    Int
    String
    Boolean
    Float
    Double
    Long
    Char
    Byte
    Short

    var a : Int // This is how we declare a variable in kotlin
    var b = 11 // We will use this if we have to assign a value directly, so it uses smartcast so that we don't have to define a data type
    a = 7
    print("value of a and b is $a and $b") // $ is used to fetch the value after it
}